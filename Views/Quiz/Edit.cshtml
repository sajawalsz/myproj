@model fyp.Models.Quiz

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

<div class="container">
    <div id="loginbox" style="margin-top:50px;" class="mainbox col-md-6 col-md-offset-3 col-sm-8 col-sm-offset-2">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            @Html.ValidationSummary(true)

            <fieldset>
                <legend>Quiz</legend>
                @Html.HiddenFor(model => model.Id)
                <div class="editor-label">
                    @Html.LabelFor(model => model.QuizName)
                </div>
                <div class="editor-field">
                    @Html.EditorFor(model => model.QuizName)
                    @Html.ValidationMessageFor(model => model.QuizName)
                </div>
                <div class="editor-label">
                    @Html.LabelFor(model => model.QuizTopic)
                </div>
                <div class="editor-field">
                    @Html.EditorFor(model => model.QuizTopic)
                    @Html.ValidationMessageFor(model => model.QuizTopic)
                </div>
                <div class="editor-label">
                    @Html.LabelFor(model => model.UserName)
                </div>
                <div class="editor-field">
                    @Html.EditorFor(model => model.UserName)
                    @Html.ValidationMessageFor(model => model.UserName)
                </div>
                <p>
                    <br />
                    <button type="submit" class="btn btn-default">Save</button>
                </p>
            </fieldset>
    <div>
        @Html.ActionLink("Back to List", "Index")
    </div>
        }
        </div>
    </div>


@section Scripts {
    
}
